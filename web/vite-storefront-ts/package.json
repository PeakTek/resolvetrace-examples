{
  "name": "@peaktek/resolvetrace-example-vite-storefront-ts",
  "version": "0.1.0",
  "private": true,
  "description": "Deliberately-buggy storefront SPA on @peaktek/resolvetrace-sdk: automatic JS + API error capture, user-reported problems, and whole-session replay across page navigations.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8080",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
